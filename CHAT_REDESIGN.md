# Chat 界面重构方案 — 决策记录

> 日期：2026-06-05
> 状态：已确认，待实施

---

## 一、核心目标

将现有表单式 UI（Playground + Storyboard）重构为**类似 Gemini 的对话式界面**，支持多对话、图片生成、视频分析与生成、素材存储库。

---

## 二、交互架构

### 2.1 页面结构

- **Chat 页面**（`/`）：替代现有 Playground + Storyboard，成为主入口
- **素材库页面**（`/gallery`）：改造现有 Gallery，升级为全局素材库
- **用量统计页面**（`/usage`）：保留，去除前端费用预估显示
- **设置页面**（`/settings`）：保留

### 2.2 对话管理

- 左侧对话列表，支持新建、切换、删除对话
- 对话类型在**创建时固定**：「视频对话」或「图片对话」
- 类型不可切换，参数选项随类型变化

### 2.3 视觉风格

- **Gemini 风格**：浅色主题、简洁干净、圆角卡片
- 左侧对话列表 + 右侧对话区
- 素材展示借鉴瀑布流卡片（Pinterest/花瓣风格），大图预览

---

## 三、图片对话流程

### 3.1 核心流程（两步式）

```
用户发送：产品图片 + 文字意图（如"生成美国独立日主题"）
        ↓
Step 1: AI 分析图片（Qwen3-VL-Plus）→ 输出：
        - 多条不同用途的提示词（主图、详情页、社交媒体图等）
        - 用户可编辑、选择想要的提示词
        ↓
Step 2: 用户确认 → 调用 OpenAI image2 生成图片
```

### 3.2 参考图机制

- 产品图片作为参考，**产品样子不变**，更换背景/场景
- 生成的图片可点击引用，附加到下一条消息

### 3.3 多图生成

- AI 建议多条不同用途的提示词（如：电商主图、产品详情页、社交媒体图）
- 用户可勾选想要的、编辑不满意的
- 一次确认后批量生成

### 3.4 参数调整

| 参数 | 选项 |
|------|------|
| 比例 | 1024×1024、1536×1024、1024×1536 |
| 画质 | low / medium / high / auto |
| 格式 | PNG / JPEG / WebP |
| 数量 | 1-4 张 |

---

## 四、视频对话流程

### 4.1 核心流程（对话式）

```
用户发送：视频 URL 或上传视频 + 意图（如"帮我做一个相似的爆款视频"）
        ↓
Step 1: AI 回复（同一条消息包含）：
        ├── 爆款分析（核心 5-6 维度，中文）
        └── 分镜拆解（AI 建议数量，可调整）+ Seedance 提示词
        ↓
Step 2: 用户审核编辑分镜提示词 → 确认生成
        ↓
Step 3: 一次生成一个完整视频（Seedance 2.0）
```

### 4.2 爆款分析框架

基于「视频广告素材全景分析体系 22 大维度」，默认使用核心维度：

| 默认维度 | 分析内容 |
|---------|---------|
| 内容结构 | AIDA/PAS/FAB 模型，黄金3秒开头，转场设计 |
| 情绪触点 | 情绪光谱、情绪转折设计、情绪密度 |
| 卖点呈现 | 功能卖点 vs 情境卖点、卖点排序逻辑 |
| 话术类型 | 理性/感性/恐惧/社交/价值观 五大说服策略 |
| 节奏与信息密度 | 信息节奏图、关键分析指标 |
| 转化优化 | 转化路径、六大促单话术模式 |

用户可追问扩展到其他维度（如：竞品对标、人称代词分析、否定表达分析等）。

### 4.3 分镜数量

- AI 根据视频时长/内容自动建议分镜数量
- 用户可手动调整（增加/减少分镜）
- 一次生成一个完整视频

### 4.4 参数调整

| 参数 | 选项 |
|------|------|
| 比例 | 16:9、9:16、1:1 |
| 时长 | 1-10 秒/镜头 |
| Seed | 可选 |

---

## 五、素材存储库

### 5.1 对话内素材

- 对话里所有上传的图、生成的图/视频，以卡片形式展示
- 可点击引用，附加到下一条消息

### 5.2 全局素材库（改造 Gallery）

- 默认**按时间线**排列
- 支持**按对话分组查看**
- 支持筛选：类型（图片/视频）、日期范围、对话来源
- 支持搜索：按提示词关键词
- 暂不做标签功能

---

## 六、AI 模型配置

- 所有视觉分析任务统一使用 **Qwen3-VL-Plus**
- 视频分镜拆解、图片分析→提示词、爆款视频分析，均用同一模型
- 配置方式：Settings 页面新增/修改千问 API Key 时指定模型

---

## 七、费用策略

- 生成前**不显示预计费用**（避免降低用户生成动力）
- 保留**统计账单页**（Usage 页面），展示总费用、按 provider 统计等

---

## 八、对话存储

- 对话记录持久化到后端数据库（SQLite）
- 新增数据库表：conversations、messages
- 支持关闭浏览器后恢复对话

---

## 九、技术决策

| 决策项 | 选择 |
|--------|------|
| 前端框架 | React 18 + TypeScript + Vite |
| UI 组件库 | shadcn/ui（扩展现有） |
| 状态管理 | 继续用 useState/useEffect，必要时引入 Context |
| 后端框架 | FastAPI（不变） |
| 数据库 | SQLite（不变，新增表） |
| AI 模型 | Qwen3-VL-Plus（DashScope OpenAI 兼容 API） |
| 图片生成 | OpenAI gpt-image-1（image edit API，支持参考图） |
| 视频生成 | Seedance 2.0（火山引擎 Ark API） |
